server {
  listen 4200;
  server_name _;

  # Serve app at / (Tailscale Serve strips the /nodi prefix when proxying)
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
}
